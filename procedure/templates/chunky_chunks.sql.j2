-- ============================================================================
-- chunky_chunks
-- Ingestion Engine.
-- Commands: ingest, list_chunks, update_chunk, delete_chunks, revert
-- Handler source: procedure/utils/chunky_chunks_handler.py
-- ============================================================================
CREATE OR REPLACE PROCEDURE chunky_chunks(
    command VARCHAR,
    instruction VARIANT
)
RETURNS VARIANT
LANGUAGE PYTHON
RUNTIME_VERSION = '{{PYTHON_RUNTIME}}'
RESOURCE_CONSTRAINT = (architecture = 'x86')
IMPORTS = (
    '{{LIB_STAGE}}/{{UTILS_BUNDLE}}',
    '{{LIB_STAGE}}/{{POPPLER_BUNDLE}}'
)
PACKAGES = ('snowflake-snowpark-python', 'pandas', 'pypdf', 'pillow')
HANDLER = 'run'
EXECUTE AS CALLER
AS
$$
from chunky_utils.chunky_chunks_handler import run
$$;
