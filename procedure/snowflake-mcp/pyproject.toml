[project]
name = "snowflake-mcp"
version = "0.1.0"
description = "Snowflake MCP Server — file upload to stage"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "snowflake-connector-python",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
