__pycache__/
*.log

# Local development
chunky_local.db
chunky_local.db-wal
chunky_local.db-shm
*.db

# Local PDF storage
local_pdfs/

# Temp files
.openclaw/
.DS_Store
*.pyc

# Credentials / PATs — never commit
.pat
*.pat
.github_pat
.env
.snowflake-mcp.json
.chunky-upload.json

# Snowflake connector token cache (~/.snowflake/ is outside the repo,
# but if anyone copies it in by accident we want it ignored)
sf_token_cache/

# Chunky deploy toolchain — local identity + build artifacts
procedure/deploy/config.json
procedure/build/out/
procedure/.smoke/
.venv-proc/
# Generated Chunky procedure bundles and local smoke artifacts
procedure/build/out/
procedure/.smoke/
procedure/deploy/config.json
