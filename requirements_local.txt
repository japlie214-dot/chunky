# requirements_local.txt for Chunky Local Development
# Mirrors Snowflake environment as closely as possible
# No Snowflake dependencies required

streamlit>=1.40.0
pandas>=1.4.0
numpy>=1.21.0
plotly>=5.6.0
pypdf>=3.17.0
Pillow>=9.2.0
mistletoe>=0.12.0
pdf2image>=1.16.0
pyarrow>=8.0.0

# System dependency: poppler (required by pdf2image)
# Linux:   sudo apt-get install poppler-utils
# macOS:   brew install poppler
# Windows: conda install -c conda-forge poppler
#          OR download from https://github.com/osber/poppler-windows/releases
#          Default install path: C:\poppler\poppler-26.02.0\Library\bin
#          If poppler is not on PATH, set the path in your code:
#            from pdf2image import convert_from_bytes
#            convert_from_bytes(pdf_bytes, poppler_path=r"C:\poppler\poppler-26.02.0\Library\bin")
